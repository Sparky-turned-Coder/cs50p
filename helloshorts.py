# So let's define a function called 'main'. Currently, it takes no inputs (parenthesis are empty).
def main():
    print("Hello, World.")
    print("This is CS50.")


# Below, we actually 'call' the function above, executing whatever that function contains.
main()
